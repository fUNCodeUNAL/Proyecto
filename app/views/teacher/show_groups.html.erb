<div>
	<br>Nombre: <%= @user.full_name %>
</div>
<div>
Email: <%= @user.email %>
</div>

<h3> Tiene los siguientes Grupos: </h3>

<table class = "table table-bordered table-hover">

	<tr>
		<th class="text-center">Nombre</th>
		<th class="text-center">Número de estudiantes</th>
	</tr>

	<% @teacher.groups.each do |group| %>
		<tr>
			<td class="text-center">
				<%= group.name %>
			</td>

			<td class="text-center">
				<%= group.students.count %>

				<div class="pull-right">
					<a href="" class="btn btn-success btn-xs">
						<span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
					</a>
					<%= link_to raw("<span class='glyphicon glyphicon-remove' aria-hidden='true'></span>"), 'groups/'+group.id.to_s, class: "btn btn-danger btn-xs", method: :delete, data: { confirm: 'Are you sure?' } %>
				</div>
				
			</td>
			<!-- <%= button_to 'Delete group', 'groups/'+group.id.to_s, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %> -->
		</tr>
	<% end %> 

</table>



<%= button_to "Add group", 'groups/new', params: {teacher_id: @user.email}, class: "btn btn-default"  %>